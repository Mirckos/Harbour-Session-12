services:
  inference-api:
    build: .
    container_name: session12-assignment-api
    ports:
      - "8015:8000"
    environment:
      MODEL_URI: ${MODEL_URI:-demo://keyword-bot}
      MLFLOW_TRACKING_URI: ${MLFLOW_TRACKING_URI:-}
