services:
  inference-api:
    build: .
    container_name: session12-monolith-api
    ports:
      - "8012:8000"
    environment:
      MODEL_NAME: keyword-bot-monolith
      MODEL_VERSION: baked-v1
